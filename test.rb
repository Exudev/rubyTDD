require "./app"
describe "temperature conversion functions" do
  describe "#ftoc" do
    it "converts freezing temperature" do
      expect(ftoc(32)).to eq(0)
    end
  end
    describe "temperature conversion functions" do
      describe "#ctof" do
        it "converts from celcius to fahrenheit tempeture" do
          expect(ctof(25)).to eq(77)
  end
end
end

 